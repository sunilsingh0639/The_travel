import { GstinValidatorDirective } from './gstin-validator.directive';

describe('GstinValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GstinValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
