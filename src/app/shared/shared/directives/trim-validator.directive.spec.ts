import { TrimValidatorDirective } from './trim-validator.directive';

describe('TrimValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
